import 'package:flutter/material.dart';

class Style
{
  static const largeSize = TextStyle(
    color: Colors.black,
    fontSize: 25.0,
  );
  static const smallSize = TextStyle(
    color: Colors.black87,
    fontSize: 16.0,
  );

  static const navBar = TextStyle(
    color: Colors.white,
    backgroundColor: Colors.blueAccent,
  );
}